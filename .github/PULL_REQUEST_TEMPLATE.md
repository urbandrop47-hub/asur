## What

<!-- One sentence: what does this PR change? -->

## Why

<!-- Why is this change needed? Link the sprint task (e.g. S18-T1) or issue. -->

## Test plan

- [ ] Backend builds clean (`pnpm -C apps/backend build`)
- [ ] Web builds clean (`pnpm -C apps/web build`)
- [ ] Manually tested the changed flow in dev (`pnpm dev`)

## Notes

<!-- Anything reviewers should know: migrations, env var changes, follow-up TODOs -->
